#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int cents;

    do
    {
        /* code */
    } 
    while (cents < 0);

}
 

int quarters = (25); 
int dimes = (10);
int nickels = (5);
int pennies = (1);